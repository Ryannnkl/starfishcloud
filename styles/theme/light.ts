import { createTheme } from '../../stitches.config';

export const light = createTheme({
	colors: {
		background: '#fffffe',
    backgroundAlt: '#eff0f3',
    primary: '#ff8e3c',
		secondary: '#eff0f3',
		text: '#0d0d0d',
		textSecondary: '#444',
		error: '#ea2423',
	},
});
